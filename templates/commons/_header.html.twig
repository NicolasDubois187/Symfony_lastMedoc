<header>
    <nav>
        <ul class="flex around align">
            <li><a href="{{ path('app_home') }}"><img src="{{ asset('assets/img/perso.jpg') }}" alt=""></a></li>
            <li><a href="{{ path('app_home') }}">home</a></li>
            <li><a href="{{ path('app_blog_index') }}">Blog</a></li>
            {% if is_granted("ROLE_ADMIN") %}
            <li><a href=""></a>admin</li>
            {% endif %}
            {% if is_granted("IS_AUTHENTICATED_REMEMBER") %}
            <li><a href="">login</a></li>
            {% else %}
            <li><a href="">logout</a></li>
            {% endif %}
        </ul>
    </nav>
</header>